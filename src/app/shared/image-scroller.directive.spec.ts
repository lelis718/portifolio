import { ImageScrollerDirective } from './image-scroller.directive';

describe('ImageScrollerDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageScrollerDirective();
    expect(directive).toBeTruthy();
  });
});
